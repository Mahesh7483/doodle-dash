# Render Blueprint: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: doodle-dash
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "22"
